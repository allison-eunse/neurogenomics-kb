# Neurogenomics KB

TOC: auto-generated

Owners:
- Allison Eun Se You
- Collaborators TBD

Scope:
- Genetics foundation models
- Brain foundation models
- Multimodal integration anchored on UK Biobank

Contribution rules:
- Keep PRs small; include references and cite upstream code/weights.
- Mark unverifiable claims as `TBD` until reviewed.
- Use `datasets/` and `schemas` content as the single source of truth for columns/field IDs.
- Add entries under `docs/decisions` for any tradeoffs or architectural calls.
