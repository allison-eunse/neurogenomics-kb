id: integration_slug
name: Integration name
summary: |
  Why this pipeline exists and the decision context.
models:
  - model_id_here
  - another_model
datasets:
  - dataset_id
pipelines:
  - describe scripts/configs/notebooks
status: designing
outputs:
  - path: outputs/example
    description: artifact
ci_checks:
  - python scripts/manage_kb.py validate models
owner: Owner name
verified: false
last_updated: 2025-01-01

